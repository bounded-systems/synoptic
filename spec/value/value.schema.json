{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bounded.tools/synoptic/spec/value/value.schema.json",
  "title": "Value",
  "description": "A canonical CSS value atom, typed. Every extracted value carries a $type (a typing claim) and validates against that type's schema — DTCG-aligned, each grounded in its CSS spec. All are proofType=derivable (resolved values, per value-stages.md); declared values are the axioms. See GROUNDING.md.",
  "oneOf": [
    { "$ref": "color.schema.json" },
    { "$ref": "dimension.schema.json" },
    { "$ref": "number.schema.json" },
    { "$ref": "percentage.schema.json" },
    { "$ref": "fontFamily.schema.json" },
    { "$ref": "keyword.schema.json" },
    { "$ref": "shadow.schema.json" }
  ]
}

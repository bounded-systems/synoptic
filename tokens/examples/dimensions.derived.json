{
  "dimension": {
    "$description": "Derived dimensions — rem atoms LOCKED to rem (px/em/ch unconstructable: they fail Resize 1.4.4 / Reflow 1.4.10), on a geometric (Weber-JND) step. $root is the fluid clamp() every rem floats on; $measure is the ch reading-width token (≤80ch, 1.4.8). AAA floors noted per atom (2.75rem target 2.5.5, 0.125rem focus 2.4.11).",
    "$root": {
      "$type": "root-font-size",
      "$css": "clamp(1rem, 0.5rem + 0.5vw, 1.25rem)",
      "$floor": {
        "$type": "CSSUnitValue",
        "value": 1,
        "unit": "rem"
      },
      "$cap": {
        "$type": "CSSUnitValue",
        "value": 1.25,
        "unit": "rem"
      },
      "$fluid": true,
      "$description": "Root = clamp(1rem, 0.5rem + 0.5vw, 1.25rem) — a CSSMathClamp. Fluid with the viewport, but FLOORED at the user's 1rem (respects Resize 1.4.4) and capped at 1.25rem. Half the preferred term is rem, so it also scales with the user's own font-size setting."
    },
    "$measure": {
      "$type": "measure",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 66,
        "unit": "ch"
      },
      "$ceiling": {
        "$type": "CSSUnitValue",
        "value": 80,
        "unit": "ch"
      },
      "$sha": "4324cbdc0901",
      "$description": "Reading width 66ch (max 80ch — WCAG 1.4.8 AAA). In ch so ~66 characters per line holds across font sizes; apply as max-width on text containers. One token, not a scale."
    },
    "rem-0_135": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 0.135,
        "unit": "rem"
      },
      "$sha": "23ebe949dacf",
      "$description": "0.135rem — 2.16px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-0_233": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 0.233,
        "unit": "rem"
      },
      "$sha": "148643bab12b",
      "$description": "0.233rem — 3.73px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-0_482": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 0.482,
        "unit": "rem"
      },
      "$sha": "cd730c4b2e16",
      "$description": "0.482rem — 7.71px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-0_694": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 0.694,
        "unit": "rem"
      },
      "$sha": "c7b98859dee4",
      "$description": "0.694rem — 11.1px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-1": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 1,
        "unit": "rem"
      },
      "$sha": "b8be0c77cfdc",
      "$description": "1rem — 16px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-1_44": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 1.44,
        "unit": "rem"
      },
      "$sha": "7f5e28d1264f",
      "$description": "1.44rem — 23.04px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-2_074": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 2.074,
        "unit": "rem"
      },
      "$sha": "9daa64e453cc",
      "$description": "2.074rem — 33.18px at the default root (font-size: medium), and it scales with the user's setting. clears the 2px minimum focus outline (WCAG 2.4.11). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-2_986": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 2.986,
        "unit": "rem"
      },
      "$sha": "21c707f21046",
      "$description": "2.986rem — 47.78px at the default root (font-size: medium), and it scales with the user's setting. large enough for a 44px minimum target (WCAG 2.5.5 AAA). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    },
    "rem-4_3": {
      "$type": "dimension",
      "$value": {
        "$type": "CSSUnitValue",
        "value": 4.3,
        "unit": "rem"
      },
      "$sha": "c9cfdd883949",
      "$description": "4.3rem — 68.8px at the default root (font-size: medium), and it scales with the user's setting. large enough for a 44px minimum target (WCAG 2.5.5 AAA). A rem, so it resizes and reflows (1.4.4 / 1.4.10) by construction — never a fixed px."
    }
  }
}
